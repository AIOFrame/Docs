<?php get_comp('head'); ?>
<article>
    <h1>Reset CSS</h1>
    <p>AIO comes with a custom made reset styles css.</p>
    <p>For those who do not know, Reset css is usually a set of CSS rules that resets the styling of all HTML elements to a consistent baseline</p>
    <p>Instead of making it all to consistent, our reset eradicates the most common overwrites that you use.</p>
    <p>Our reset style also gives a basic interface to input elements with minimal code.</p>
    <div class="mb60"></div>
    <h2>Use Reset CSS:</h2>
    <p>In your head you can use following php function to render link to reset stylesheet.</p>
    <div class="code">
        <pre class="dib">reset_styles();</pre>
    </div>
</article>
<?php get_comp('foot');